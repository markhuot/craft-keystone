{% if asset|default(false) %}
    {% if asset.getExtension() == 'svg' %}
        {{ asset.getContents()|attr(attributes.toArray())|raw }}
    {% else %}
        <img {{ attributes }} src="{{asset.getUrl(transform|default(null)) }}">
    {% endif %}
{% endif %}
